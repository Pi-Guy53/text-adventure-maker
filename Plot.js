class Plot {
    constructor(text, id, sprite, txtBox, x, y) {
        this.text = text,
            this.id = id,
            this.sprite = sprite,
            this.txtBox = txtBox,
            this.x = x,
            this.y = y
    }
}